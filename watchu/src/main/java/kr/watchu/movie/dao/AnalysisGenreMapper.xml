<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper   
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
<mapper namespace="kr.watchu.movie.dao.AnalysisGenreMapper">
	<insert id="insertGenreRate" parameterType="arraylist">
		INSERT INTO analysis_genre (movie_num,id,genre,rate,reg_date)
		 VALUE
		 <foreach collection="">
		 (#{movie_num},#{id},#{genre},#{rate},sysdate)
		 </foreach>
	</insert>
	<select id="selectList">
	</select>
	<select id="selectCnt">
	
	</select>
</mapper>